# npm 12 默认禁止拉取 git 类型依赖(EALLOWGIT); 本仓库的 zotero-types 依赖链里
# 有两个 git fork(pdfjs-dist、epub.js, 均为 zotero 官方组织的公开仓库,
# commit 由 package-lock.json 锁定)。npm 12 的 allow-git 只接受 all/none/root,
# git 依赖是传递依赖故 root 不适用, 只能放行 all
allow-git=all
