/**
 * Bootstrap stub — replaced at runtime by Zotero's plugin loader.
 * Exists so tsc project references resolve; real entry is src/index.ts
 * bundled to content/scripts/smarttranslate.js and loaded by
 * addon/bootstrap.js via Services.scriptloader.loadSubScript.
 */
export {};
